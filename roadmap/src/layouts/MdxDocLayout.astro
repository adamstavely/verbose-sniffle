---
import BaseLayout from './BaseLayout.astro';

interface Props {
  frontmatter: {
    title: string;
    description?: string;
    /** Wrap content in prose typography styles. Set false for hand-styled MDX (e.g. the style guide). */
    prose?: boolean;
  };
}

const { frontmatter } = Astro.props;
const {
  title,
  description = 'Product roadmap - documentation and guides',
  prose = true,
} = frontmatter;
---

<BaseLayout title={title} description={description}>
  {prose ? (
    <article class="doc-prose">
      <slot />
    </article>
  ) : (
    <slot />
  )}
</BaseLayout>

<style is:global>
  .doc-prose { color: rgb(51 65 85); }
  .doc-prose h1 { font-size: 1.875rem; line-height: 2.25rem; font-weight: 700; color: rgb(15 23 42); margin-bottom: 1rem; }
  .doc-prose h2 { font-size: 1.25rem; line-height: 1.75rem; font-weight: 600; color: rgb(15 23 42); margin-top: 2rem; margin-bottom: 0.75rem; }
  .doc-prose h3 { font-size: 1.0625rem; font-weight: 600; color: rgb(30 41 59); margin-top: 1.5rem; margin-bottom: 0.5rem; }
  .doc-prose p { margin-bottom: 1rem; line-height: 1.7; }
  .doc-prose ul { list-style: disc; padding-left: 1.5rem; margin-bottom: 1rem; }
  .doc-prose ol { list-style: decimal; padding-left: 1.5rem; margin-bottom: 1rem; }
  .doc-prose li { margin-bottom: 0.35rem; }
  .doc-prose a { color: rgb(79 70 229); text-decoration: underline; text-underline-offset: 2px; }
  .doc-prose a:hover { color: rgb(67 56 202); }
  .doc-prose strong { font-weight: 600; color: rgb(15 23 42); }
  .doc-prose code { background: rgb(241 245 249); border-radius: 0.25rem; padding: 0.1rem 0.35rem; font-size: 0.85em; }
  .doc-prose pre { border-radius: 0.5rem; padding: 1rem; overflow-x: auto; margin-bottom: 1rem; }
  .doc-prose pre code { background: transparent; padding: 0; font-size: 0.85em; }
</style>
